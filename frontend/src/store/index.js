import { createStore } from "vuex";

import menu from "./modules/menu";

const modules = {
  menu
};

const state = {};

const mutations = {};

const getters = {};

const actions = {};

export default createStore({
  modules,
  state,
  mutations,
  getters,
  actions
});
